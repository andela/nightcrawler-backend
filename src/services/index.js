import * as UserServices from './users';

/**
 * This file handles all application services
 * To add another module service, import the module folder as above,
 * add it to the export statement
 * Remember to remove the default keyword since you are exporting two or more variables
 */
export default {
  UserServices
};
